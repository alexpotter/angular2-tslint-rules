version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - restore_cache:
          keys:
            - npm-dependencies-{{ checksum "package-lock.json" }}
            - deps-
      - run: npm install
      - save_cache:
          key: npm-dependencies-{{ checksum "package-lock.json" }}
          paths: 'node_modules'
      - run:
          name: Deploy
          command: ./.circlci/deploy.sh $CIRCLE_BRANCH $NPM_TOKEN
